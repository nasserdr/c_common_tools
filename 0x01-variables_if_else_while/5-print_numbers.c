#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/*
 *A program that prints the alphabet in lowercase, followed by a new line.
 */

int main(void)
{
	int a;
	for ( a = 0; a <= 9; a++)
		printf("%d",a);
	printf("\n");
	return (0);
}
